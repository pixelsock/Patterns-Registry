{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "pattern-sonner-promise-2",
  "type": "registry:component",
  "title": "Sonner Promise 2",
  "description": "A sonner pattern: sonner-promise-2",
  "registryDependencies": [],
  "files": [
    {
      "path": "registry/new-york/patterns/sonner/promise/sonner-promise-2.tsx",
      "content": "\"use client\";\n\nimport { toast } from \"sonner\";\nimport { Button } from \"@/components/ui/button\";\n\nexport const title = \"Promise Toast with Data\";\n\nconst Example = () => {\n  const handleClick = () => {\n    const promise = new Promise<{ name: string }>((resolve) => {\n      setTimeout(() => resolve({ name: \"John Doe\" }), 2000);\n    });\n\n    toast.promise(promise, {\n      loading: \"Creating user...\",\n      success: (data) => `${data.name} has been added`,\n      error: \"Failed to create user\",\n    });\n  };\n\n  return (\n    <Button onClick={handleClick} variant=\"outline\">\n      Create User\n    </Button>\n  );\n};\n\nexport default Example;\n",
      "type": "registry:component"
    }
  ]
}